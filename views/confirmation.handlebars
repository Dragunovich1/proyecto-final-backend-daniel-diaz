<!DOCTYPE html>
<html>
<head>
    <title>Producto Agregado</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/milligram/1.4.1/milligram.min.css">
    <style>
        .container {
            width: 80%;
            margin: 0 auto;
            padding: 20px;
            max-width: 800px;
            margin-top: 30px;
        }
        .message {
            background-color: #dff0d8;
            color: #3c763d;
            padding: 1rem;
            border: 1px solid #d6e9c6;
            border-radius: 3px;
            margin-bottom: 1rem;
        }
        .product-details {
            background-color: #f8f8f8;
            padding: 1rem;
            border: 1px solid #ddd;
            border-radius: 3px;
        }
        .action-link {
            padding: 0.5rem 1rem;
            background-color: #333;
            color: #fff;
            text-decoration: none;
            border-radius: 3px;
            text-align: center;
            cursor: pointer;
            margin-top: 1rem;
            display: inline-block;
        }
        .action-link:hover {
            background-color: #555;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="message">
            Producto agregado exitosamente.
        </div>
        <div class="product-details">
            <pre>{{json product}}</pre>
        </div>
        <a href="/carts/{{cart._id}}" class="action-link">Ver Carrito</a>
    </div>
</body>
</html>
